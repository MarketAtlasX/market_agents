version: '3.8'
services:
  neo4j:
    image: neo4j:5
    environment:
      - NEO4J_AUTH=neo4j/test
    ports:
      - "7474:7474" # http
      - "7687:7687" # bolt
    volumes:
      - ./neo4j_data:/data
